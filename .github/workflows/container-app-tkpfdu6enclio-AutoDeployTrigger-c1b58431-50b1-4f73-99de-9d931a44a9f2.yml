name: Trigger auto deployment for container-app-tkpfdu6enclio

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-tkpfdu6enclio-AutoDeployTrigger-c1b58431-50b1-4f73-99de-9d931a44a9f2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPTKPFDU6ENCLIO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPTKPFDU6ENCLIO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPTKPFDU6ENCLIO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistrytkpfdu6enclio.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPTKPFDU6ENCLIO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPTKPFDU6ENCLIO_REGISTRY_PASSWORD }}
          containerAppName: container-app-tkpfdu6enclio
          resourceGroup: learnclouddemo2ad10
          imageToBuild: containerregistrytkpfdu6enclio.azurecr.io/container-app-tkpfdu6enclio:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


